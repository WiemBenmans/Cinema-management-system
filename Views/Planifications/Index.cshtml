@model IEnumerable<Planification>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr> 
             <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Film)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prixticket)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.id)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Film.idFilm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salle.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDate)
            </td> 
                 <td>
                    @Html.DisplayFor(modelItem => item.prixticket)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
